type Mission {
	id: String!
	name: String!
	description: String!
	tasks: [Task]
}

input MissionInput {
	name: String!
	description: String!
	tasks: [TaskInput]
}

type Query {
	getMissions: [Mission]
}

type Mutation {
	addMission(mission: MissionInput): Mission
}
