type Task {
  id: String!
  name: String!
  description: String!
  link: String
}

input TaskInput {
  name: String!
  description: String!
  link: String
}

type Query {
  tasks: [Task]
}

type Mutation {
  addTask(task: TaskInput): Task
}
