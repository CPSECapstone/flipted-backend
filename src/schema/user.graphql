type User {
  id: String!
  firstName: String!
  lastName: String!
}

input UserInput {
  firstName: String!
  lastName: String!
}

type Query {
  getUsers: [User]
}

type Mutation {
  addUser(user: UserInput): User
}
